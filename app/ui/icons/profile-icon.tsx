import AccountCircleIcon from '@mui/icons-material/AccountCircle';
export default function ProfileIconComponent({ color }: { color: string }) {
  return (
    <AccountCircleIcon style={{ color }} />
  );
}
